enum TokenType {
    TEXT = 1,
    WHITESPACE = 2,
    LEFT_BRACE = "{",
    RIGHT_BRACE = "}",
    BACKTICK = "\`",
    START_TAG = "<",
    END_TAG = "\/>",
    COLON = ":",
    PIPE = "|",
    SEMICOLON = ";" 
}

export default TokenType